# Appwrite (same project as the Monochrome client)
APPWRITE_ENDPOINT=https://sfo.cloud.appwrite.io/v1
APPWRITE_PROJECT_ID=69c700650032a44907ed

# Vercel KV (create in Vercel project → Storage → KV)
# After creating KV, link to project — Vercel injects these automatically:
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
# KV_URL= (optional, older)
